# MCP Server Template

This is a template for creating MCP (Model Context Protocol) servers.

## When helping with MCP development:

- Always reference `docs/mcp-typescript-sdk.txt` for SDK API usage and patterns
- Always reference `docs/mcp-docs.txt` for protocol specifications